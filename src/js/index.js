class Button {
  constructor(name, color, width, height, padding, margin) {
    this.name = name;
    this.color = color;
    this.width = width;
    this.height = height;
    this.padding = padding;
    this.margin = margin;
  }
}
const SignInButton = new Button();
console.log(SignInButton);
